{
    "name": "selenium-mocha-digital-skola",
    "version": "1.0.0",
    "description": "Selenium WebDriver uji using Mocha with Mochawesome reports",
    "scripts": {
        "test-sauce": "mocha uji/test-sauce.js",
        "cobain": "mocha uji --recursive --timeout 60000",
        "test-coba": "mocha uji/sesi-9/test_sesi9.js --recursive --timeout 60000",
        "test-parallel": "mocha uji --parallel --recursive --timeout 60000",
        "test-pertama": "mocha uji --grep 'Pertama' --recursive --timeout 60000",
        "jalanin-mochawesome": "mocha uji --recursive --timeout 60000 --reporter mochawesome",
        "jalanin-simple-html": "mocha uji --recursive --timeout 60000 --reporter mocha-simple-html-reporter --reporter-options output=report.html",
        "jalanin-report-excel": "mocha uji --recursive --timeout 60000 --reporter excel-reporter.js"
    },
    "dependencies": {
        "selenium-webdriver": "^4.17.0"
    },
    "devDependencies": {
        "exceljs": "^4.4.0",
        "mocha": "^10.4.0",
        "mocha-simple-html-reporter": "^2.0.0",
        "mochawesome": "^7.1.3"
    }
}
